#include <stdio.h>


int main(void){

    // assign the value of i as 972
    // then compare if i is greater than and equals to 897
    // if its greater than decrease the value of i by 1;
    // if it's smaller than limit than stop the loop
    for(int i = 972;i>=897;i--){
        
        printf("%d\n",i);
    }

    
    return 0;
}